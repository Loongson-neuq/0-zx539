#include ".assignment/test.h"           // 比较三个数的大小
struct Result {
    int min;
    int mid;
    int max;
};
void CompareThreeNumbers(int a, int b, int c, Result &result)
{
    a=1;
    b=3;
    C=6;
    result.min =;
    result.mid =;
    result.max =;
                                               // 在这里完成你的代码
}
